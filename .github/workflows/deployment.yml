name: GitHub-CICD-Pipeline
on:
  push:
    branches: [main]
jobs:
  scan:
    name: gitleaks
    runs-on: 
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
